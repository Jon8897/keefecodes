# V1.0.0.5
name: KeefeCodes DevSecOps Pipeline

on:
  push:
    branches:
      - dev
      - main

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.5'

      - name: Install dependencies
        run: npm ci

      - name: Lint (code quality check)
        run: npm run lint --if-present || true
      - name: Build project
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  security-scan:
    name: Security Audit
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm ci

      - name: Run npm audit
        run: npm audit --audit-level=moderate || true 

  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    needs: [build-and-test, security-scan]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download built artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Write SSH Private Key
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' > /tmp/private_key
          chmod 600 /tmp/private_key

      - name: Add server to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.REG_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy dist folder via SSH
        run: |
          BRANCH=${GITHUB_REF##*/}
          echo "Deploying branch: $BRANCH"

          if [ "$BRANCH" == "dev" ]; then
            TARGET_DIR="/opt/apps/keefecodes/dev"
          elif [ "$BRANCH" == "main" ]; then
            TARGET_DIR="/opt/apps/keefecodes/prod"
          else
            echo "Not a deployment branch."
            exit 0
          fi

          echo "🚀 Deploying dist/ to $TARGET_DIR..."

          cat > /tmp/ssh_wrapper.sh << 'EOF'
#!/bin/bash
ssh -i /tmp/private_key -p 22 -o StrictHostKeyChecking=no -o BatchMode=yes -o LogLevel=QUIET "$@" 2>/dev/null
EOF
          chmod +x /tmp/ssh_wrapper.sh

          rsync -avz --delete \
            -e "/tmp/ssh_wrapper.sh" \
            ./dist/ ${{ secrets.REG_FTP_USERNAME }}@${{ secrets.REG_HOST }}:$TARGET_DIR \
            || (echo "❌ Deployment failed." && exit 1)
            
  notify:
    name: Discord Notification
    runs-on: ubuntu-latest
    needs: [build-and-test, security-scan, deploy]
    if: always()

    steps:
      - name: Send detailed CI/CD status to Discord
        env:
          WEBHOOK_URL: ${{ secrets.KEEFECODES_DEVOPS_BOT }}
          REPO: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
          BRANCH: ${{ github.ref_name }}
          AUTHOR: ${{ github.event.head_commit.author.name }}
          COMMIT_MSG: ${{ github.event.head_commit.message }}
          COMMIT_HASH: ${{ github.sha }}
          BUILD_STATUS: ${{ needs.build-and-test.result }}
          SECURITY_STATUS: ${{ needs.security-scan.result }}
          DEPLOY_STATUS: ${{ needs.deploy.result }}
        run: |
          if [[ "$DEPLOY_STATUS" == "success" && "$BUILD_STATUS" == "success" && "$SECURITY_STATUS" == "success" ]]; then
            COLOR=3066993
            STATUS_TEXT="✅ Deployment Succeeded"
          else
            COLOR=15158332
            STATUS_TEXT="❌ Deployment Failed"
          fi

          FAIL_REASON=""
          [[ "$BUILD_STATUS" != "success" ]] && FAIL_REASON="${FAIL_REASON}❗ Build step failed\n"
          [[ "$SECURITY_STATUS" != "success" ]] && FAIL_REASON="${FAIL_REASON}🧩 Security audit issues detected\n"
          [[ "$DEPLOY_STATUS" != "success" ]] && FAIL_REASON="${FAIL_REASON}🚫 Deployment error\n"
          [[ -z "$FAIL_REASON" ]] && FAIL_REASON="✅ All stages completed successfully"

          RUN_URL="https://github.com/$REPO/actions/runs/$RUN_ID"

          curl -H "Content-Type: application/json" \
          -X POST \
          -d "{
            \"username\": \"KeefeCodes CI/CD Bot\",
            \"embeds\": [{
              \"title\": \"$STATUS_TEXT\",
              \"color\": $COLOR,
              \"fields\": [
                {\"name\": \"Repository\", \"value\": \"$REPO\", \"inline\": true},
                {\"name\": \"Branch\", \"value\": \"$BRANCH\", \"inline\": true},
                {\"name\": \"Author\", \"value\": \"$AUTHOR\", \"inline\": true},
                {\"name\": \"Commit Message\", \"value\": \"$COMMIT_MSG\"},
                {\"name\": \"Commit Hash\", \"value\": \"[\`${COMMIT_HASH:0:7}\`]($RUN_URL)\"},
                {\"name\": \"Build Status\", \"value\": \"$BUILD_STATUS\", \"inline\": true},
                {\"name\": \"Security Scan\", \"value\": \"$SECURITY_STATUS\", \"inline\": true},
                {\"name\": \"Deployment\", \"value\": \"$DEPLOY_STATUS\", \"inline\": true},
                {\"name\": \"Summary\", \"value\": \"$FAIL_REASON\"}
              ],
              \"footer\": {\"text\": \"KeefeCodes DevSecOps Pipeline • $(date)\"}
            }]
          }" $WEBHOOK_URL